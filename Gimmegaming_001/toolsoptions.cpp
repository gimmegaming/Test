#include "toolsoptions.h"

namespace Application
{
    ToolsOptions::ToolsOptions(bool recCenter, bool recButtons, int recWidth, int recHeight, QWidget *parent) : CreateMenus(recCenter, recButtons, recWidth, recHeight, parent)
    {

    }

    ToolsOptions::~ToolsOptions()
    {

    }
}
