#include "openfile.h"

namespace Application
{
    OpenFile::OpenFile(QWidget *parent) : CreateMenus(parent)
    {
        this->setWindowTitle("Open");
    }

    OpenFile::~OpenFile()
    {

    }
}
